apiVersion: 1

datasources:
  - name: Trino Lakehouse
    type: trino-datasource
    access: proxy
    url: http://trino:8080
    isDefault: true
    jsonData:
      catalog: delta
      schema: market
    editable: true
    user: grafana
